# shIT | shellless Input Terminal

The IT chapter's new-and-improved simple interface for interacting with kistan.
`

## Development

Start the dev server just like any other project using

```bash
npm run dev
```

Before you commit, please run `npm run fix` to have your code be auto-formatted.

Also run `npm run check` to find any remaining issues with your code.
